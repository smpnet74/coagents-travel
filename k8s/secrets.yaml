apiVersion: v1
kind: Secret
metadata:
  name: travel-secrets
  namespace: app-travelexample
type: Opaque
data:
  # Base64 encoded values - will be generated from .env file during deployment
  # Use: echo -n "your-api-key" | base64
  OPENAI_API_KEY: ""  # Will be populated during deployment
  GOOGLE_MAPS_API_KEY: ""  # Will be populated during deployment